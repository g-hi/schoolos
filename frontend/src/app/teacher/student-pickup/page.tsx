import TeacherPlaceholderPage from "@/components/teacher-placeholder-page";

export default function StudentPickupPage() {
  return (
    <TeacherPlaceholderPage
      title="Student Pickup"
      description="Monitor pickup confirmations, release requests, and early dismissal activity for classes under your care."
      badge="Operations"
    />
  );
}
