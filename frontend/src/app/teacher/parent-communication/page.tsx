import TeacherPlaceholderPage from "@/components/teacher-placeholder-page";

export default function ParentCommunicationPage() {
  return (
    <TeacherPlaceholderPage
      title="Parent Communication"
      description="Send class updates, review recent conversations, and keep parent engagement visible."
      badge="Communication"
    />
  );
}
