databases:
  - name: schoolos-db
    plan: free
    databaseName: schoolos
    user: schoolos_user
    ipAllowList: []

services:
  - type: web
    name: schoolos-gateway
    runtime: docker
    repo: https://github.com/g-hi/schoolos
    branch: master
    plan: free
    dockerfilePath: ./Dockerfile
    # The Dockerfile CMD runs `alembic upgrade head` before uvicorn.
    # This ensures every migration is applied before the application
    # accepts traffic.  A migration failure stops the deployment.
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: schoolos-db
          property: connectionString
      - key: REDIS_URL
        value: ""
      - key: APP_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
